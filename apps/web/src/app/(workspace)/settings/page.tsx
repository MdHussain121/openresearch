import { SettingsView } from '../../../components/views/SettingsView';

export const metadata = { title: 'Settings — OpenResearch' };

export default function SettingsPage() {
  return <SettingsView />;
}
