import { DocumentsView } from '../../../components/views/DocumentsView';

export const metadata = { title: 'Documents — OpenResearch' };

export default function DocumentsPage() {
  return <DocumentsView />;
}
