import { LibraryView } from '../../../components/views/LibraryView';

export const metadata = { title: 'Library — OpenResearch' };

export default function LibraryPage() {
  return <LibraryView />;
}
